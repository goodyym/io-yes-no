<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.good.mapper.GoodPermissionMapper">

    <select id="getPermissionByRole" resultType="GoodPermission">
        select * from ${tableName} where role_permission_id = #{id}
    </select>

</mapper>